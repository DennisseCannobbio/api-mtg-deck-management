# Learning State

- **Current Phase:** Phase 2 EN CURSO - Clean Architecture, SOLID & Unit Testing.
- **Current Lesson:** 2.4 ✅ COMPLETADA (Application Layer / Use Cases). Arquitectura Clean completa end-to-end: Controller delgado → 4 use cases (Create/FindAll/FindById/Update) → repo vía `@Inject(CARDS_REPOSITORY)` → InMemoryCardsRepository. `UpdateCardDto` con `PartialType` (instalado `@nestjs/mapped-types`). `CardsService` viejo + `card.interface` anémica ELIMINADOS. Verificado: build OK + 10 tests verdes + la app ARRANCA (DI resuelve sin errores). Estructura por-feature (D-012). Rama: `feat/feature-based-structure` — TODO sin commitear (migración + wiring 2.3 + use cases 2.4). **Pendiente: commit + PR (opción B: todo el bloque junto).**
  - **Siguiente:** 2.5 (Unit Testing a fondo: testear use cases con el repo MOCKEADO sin interfaz — se ve el pago del "no interfaz para use case"). Luego 2.6 (cierre + cobertura). Ojo deuda: el 404 (Fase 4), validación DTO/class-validator (Fase 3).
- **Approved Syllabus (Phase 1):**
  - 1.1 Bootstrapping y anatomía (CLI, main.ts, NestFactory, AppModule)
  - 1.2 Módulos (@Module: imports/controllers/providers/exports, encapsulación)
  - 1.3 Controladores (@Controller, routing, @Param/@Query/@Body, HttpCode)
  - 1.4 Providers e Inyección de Dependencias (@Injectable, DI por constructor, scopes)
  - 1.5 Cierre e integración (custom providers, CRUD Cards en memoria)
- **Completed Milestones:**
  - PR #1 mergeado a `dev`: config del tutor (`CLAUDE.md`) + sistema de contexto (`.context/`).
  - Entorno verificado: Node v24.18.0 / npm 11.16.0.
  - Proyecto NestJS scaffoldeado con `nest new . --skip-git --package-manager npm` (en rama `feat/nestjs-scaffolding`, aún sin commitear/PR).
  - `.gitignore` creado (faltaba por `--skip-git`; `node_modules/` ya ignorado).
  - Teoría 1.1 explicada: circuito main→module→controller→service + rol de `@Injectable()` y símil con la DI de .NET. Ver `learning-notes.md`.
  - Server arrancado OK (`npm run start:dev`): "Hello World!" en localhost:3000, logs de bootstrap leídos y entendidos. **Lección 1.1 completada.**
  - **Lección 1.2 completada:** feature module `CardsModule` creado pieza por pieza (module + controller + service) en rama `feat/cards-module`. Estructura completa pero funcionalmente vacía. Teoría de módulos/encapsulación en `learning-notes.md`.
  - **Lección 1.3 completada:** 3 endpoints en `CardsController` (GET /cards, GET /cards/:id, POST /cards) con `@Param`/`@Body`, probados en Postman. Modelo de dominio `Card` rico (enums separados: color/type/rarity/super-type; arrays multicolor/multi-tipo; power/toughness opcionales string; manaValue). Lecciones: DTO vs Entidad (no heredar) y descubrimiento de que el DTO no valida en runtime (→ Pipes en Fase 3). Todo en rama `feat/cards-endpoints`.
  - **Lección 1.4 completada:** DI + CRUD en memoria funcionando end-to-end (probado en Postman: POST 201 con carta enriquecida, GET /cards persiste estado entre requests, GET /:id devuelve una). `CardsService` inyectado por constructor (parameter property `private readonly`). `interface Card` (= DTO + id UUID + auditoría createdAt/By, updatedAt/By opcional) en `src/models/interface/`. Mapeo manual DTO→Card con spread (`{ id, ...dto, createdAt, createdBy }`) dentro del service. Analogía clave con .NET: hoy `CardsService` = capa Handler/negocio; el `push` al array finge el Repository (se separa en Fase 3). Fixes: operador `!` en DTO (`ts(2564)`), `findOne` devuelve `Card | undefined`. Config editor versionada (`.vscode/settings.json`, D-007). Rama `feat/cards-crud` mergeada a `dev`.
  - **Deuda técnica DECIDIDA (404 / manejo de "no encontrado") → Fase 4 (Filters):** Cuando `findById` devuelve `undefined`, el 404 NO va en el use case (violaría capas: `NotFoundException` es de `@nestjs/common` = HTTP). Patrón elegido = **Opción A** (la que el estudiante YA usa en .NET): lanzar excepción de DOMINIO agnóstica de HTTP (ej. `CardNotFoundError` en `domain/`) + un **Exception Filter** de NestJS (`@Catch()`) que la traduce a 404. Mapeo directo con su .NET: su `RecursoNoEncontradoException` + middleware de excepciones ≡ excepción de dominio + Exception Filter en NestJS. Por ahora (2.4) el use case propaga `Card | undefined` (correcto). Se difiere a Fase 4 para no romper el foco de 2.4 y porque Filters es tema de esa fase.
  - **Idea futura (subtipos de carta / Subtypes):** Modelar los subtipos de MTG (ej. criatura "Goblin", "Elf"; encantamiento "Aura"; tierra "Island"). Referencia oficial: https://mtg.fandom.com/wiki/Subtype. POSPUESTO al final del proyecto (YAGNI por ahora — ninguna regla de Deck actual los necesita). Cuando se retome: evaluar si van como enum grande, `string[]`, o VO; y si alguna regla (ej. tribal/typal) los requiere. Solo en el radar para no olvidarlo.
  - **Idea futura (imagen de carta):** Añadir imagen a `Card`. Dos alcances distintos: (A) `imageUrl?: string` = campo de dominio trivial, buen candidato para Fase 3 junto con validación de URL (`class-validator @IsUrl`); (B) archivo binario real (upload/almacenamiento S3/Multer) = infraestructura, Fase 4. Pospuesto para no romper el foco de 2.3 (Repository Pattern).
  - **Nota de futuro (YAGNI):** El estudiante amplió `CardSuperType` con supertypes oficiales (Snow, World, Ongoing). Idea de "agregar más reglas/métodos para esos supertypes" POSPUESTA a propósito: se crearán CUANDO una regla de Deck los necesite (con TDD), no especulativamente. Ver principios YAGNI/KISS/DRY en learning-notes.
  - **Lección 2.1 completada:** Clean Architecture teórica: los 4 anillos concéntricos (Entities/Domain → Use Cases/Application → Interface Adapters → Frameworks & Drivers) y la **Dependency Rule** (dependencias solo apuntan hacia adentro; el dominio no conoce a nadie, todos conocen al dominio). El estudiante explicó correctamente el DIP con su símil .NET (handler→interfaz, no impl; el repo usa EF Core). Clave anclada: el **puerto** (`ICardsRepository`) vive en el círculo interior (dominio), el **adapter** (impl) en el exterior → Ports & Adapters / Hexagonal. Se validó qué es del libro de Uncle Bob vs convención de comunidad (ver D-009): anillos+regla=libro; nombres de carpeta=comunidad; subir la interfaz al dominio=canónico del libro. Estructura de carpetas por-capa aprobada (D-009): `domain/{entities,enums,repositories}`, `application/{dto,use-cases}`, `infrastructure/{http,persistence}`. Árbol destino diseñado (aún NO creado). Ver learning-notes.
  - **Lección 1.5 completada → FASE 1 CERRADA:** Custom Providers vistos a nivel conceptual (token ≠ implementación; formas useClass/useValue/useFactory/useExisting; símil `AddScoped<I,Impl>`). Punto clave: las interfaces TS no existen en runtime → no sirven como token de inyección; solución = token string/Symbol + `@Inject(TOKEN)` (= inyectar `IRepository` a la manera de TS). Se decidió tratarlo SOLO conceptual y implementarlo en Fase 3 al separar `CardsRepository` (evitar sobre-ingeniería). Repaso consolidado del circuito completo main→module→controller→service y las 4 "clics" de la fase (type erasure, DI por convención, separación de capas, token≠impl). Ver learning-notes.
- **Active Decisions / Notes:**
  - Temario Fase 1 aprobado como primera aproximación (se profundiza sobre la marcha).
  - Rama de trabajo actual: `feat/cards-crud`.
  - Nueva regla en CLAUDE.md: toda explicación/recomendación debe fundamentarse en documentación oficial y citar la fuente.
  - Deuda técnica anotada (para Fase 3): DTO/interface no validan en runtime (→ class-validator + ValidationPipe); `@Param('id')` tipado UUID no valida (→ ParseUUIDPipe); `findOne` sin match debe lanzar 404 (NotFoundException); `power`/`toughness` como tipo (`number` vs `string` para `*` de MTG); posible DRY `Card extends CreateCardDto`.
  - **Pendiente inmediato:** commit + push + PR (`feat/cards-crud → dev`) con el CRUD (service + controller + interface Card + `.vscode/`). Luego Lección 1.5 (cierre/integración Fase 1).
  - Nota: el archivo es `CLAUDE.md` (mayúsculas); en Windows es case-insensitive.
